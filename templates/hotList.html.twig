{% extends 'index.html.twig' %}

{% block title %}七日热门 - BCD{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" type="text/css" href="/CSS/hot-list.css">
{% endblock %}

{% block body %}
    <header>
        <nav>
            <a href="/">BilibiliCD</a>
            <a href="/">&lsaquo; BCD</a>
        </nav>
        <div id="title">
            <h1>七日热门</h1>
        </div>
    </header>
    {% for item in list %}
        <span id="noreferrer-{{ item.getStringID() }}"></span>
        <section>
            <figure>
                <img src="{{ item.getURL() }}" alt="{{ item.getTitle() }}" rel="noreferrer">
            </figure>
            <div>
                <h2>{{ item.getStringID() }}</h2>
                <h3>标题：{{ item.getTitle() }}</h3>
                <p>UP主：{{ item.getAuthor() }}</p>
                <p>封面链接：{{ item.getURL() }}</p>
            </div>
        </section>
    {% endfor %}
{% endblock %}

{% block javascripts %}
    <script src="Javascript/referrer-killer.js"></script>
    <script>
        {% for item in list %}
        document.getElementById('noreferrer-{{ item.getStringID }}').innerHTML = ReferrerKiller.imageHtml('{{ item.getURL }}');
        {% endfor %}
    </script>
{% endblock %}
